// alert('teste teste teste ...')

// Incluindo cógido ruby para mostrar o valor da variável que vem do controller.
// alert('A opção é ... "<%= @answer.correct %>"')


var element = document.getElementById('submit_<%= @answer.question_id %>');
// var x = document.getElementById('submit_1881');


element.classList.remove("btn-default");
element.classList.remove("btn-success");
element.classList.remove("btn-danger");

if (<%= @answer.correct %>) {

  element.classList.add('btn-success');

  $.bootstrapGrowl("Você acertou!", {
    type: 'success', // (null, 'info', 'danger', 'success')
    offset: {from: 'top', amount: 10}, // 'top', or 'bottom'
    align: 'right', // ('left', 'right', or 'center')
    width: 450, // (integer, or 'auto')
    delay: 8000, // Time while the message will be displayed. It's not equivalent to the *demo* timeOut!
    allow_dismiss: true, // If true then will display a cross to close the popup.
    stackup_spacing: 10 // spacing between consecutively stacked growls.
  });

} else {

  element.classList.add('btn-danger');

  $.bootstrapGrowl("Você errou!", {
    type: 'danger', // (null, 'info', 'danger', 'success')
    offset: {from: 'top', amount: 10}, // 'top', or 'bottom'
    align: 'right', // ('left', 'right', or 'center')
    width: 450, // (integer, or 'auto')
    delay: 8000, // Time while the message will be displayed. It's not equivalent to the *demo* timeOut!
    allow_dismiss: true, // If true then will display a cross to close the popup.
    stackup_spacing: 10 // spacing between consecutively stacked growls.
  });
}
